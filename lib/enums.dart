
enum VehicleType {
  Car,
  Bike,
  NumberPlate,
}

enum VehicleCondition {
  New,
  Used
}
enum PlateType {
  Private,
  Transport
}

enum NumberPlatePrivilege {
  Yes,
  No
}
enum Transmission {
  Auto,
  Manual
}
enum FuelType {
  Petrol,
  Diesel,
  Electric,
  Hybrid,
  Other
}
enum Purpose {
  Sell,
  Rent
}

enum PaymentMethod {
  Cash,
  Installments
}
enum LoginWith {
  Google,
  Facebook,
  Apple
}


  enum Status {
    initial,
    loading,
    success,
    error
  }

enum AuthState {
  authorized,
  unauthorized,
}

enum SortBy {
  defaultValue,
  priceLowToHigh,
  priceHighToLow,
  discount,
  newest,
}